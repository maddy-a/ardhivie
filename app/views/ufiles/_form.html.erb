<%= form_for(@ufile) do |f| %>
  <% if @ufile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ufile.errors.count, "error") %> prohibited this ufile from being saved:</h2>

      <ul>
      <% @ufile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
